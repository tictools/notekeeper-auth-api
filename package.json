{
  "name": "notekeeper-auth",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:watch": "NODE_ENV=development bun --watch index.ts",
    "start": "NODE_ENV=production bun index.ts",
    "test": "NODE_ENV=test echo ⚠️ Pending to implement test suite",
    "test:watch": "jest --watch",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --ext js --report-unused-disable-directives --max-warnings 0 --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@eslint/js": "^9.2.0",
    "@types/cors": "2.8.17",
    "@types/express": "4.17.21",
    "bun-types": "latest",
    "eslint": "^9.2.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "lint-staged": {
    "*.ts": "eslint --cache --fix"
  }
}
